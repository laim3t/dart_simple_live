name: build-macos-arm64

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - ".github/workflows/build_macos_arm64.yaml"

jobs:
  build-macos:
    runs-on: macos-14
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.38.x"
          cache: true

      - name: Enable Flutter Desktop
        run: flutter config --enable-macos-desktop

      - name: Restore packages
        run: |
          cd simple_live_app
          flutter pub get

      - name: Build macOS
        run: |
          cd simple_live_app
          flutter build macos --release

      - name: Package app
        run: |
          cd simple_live_app/build/macos/Build/Products/Release
          ditto -c -k --sequesterRsrc --keepParent simple_live_app.app simple_live_app-macos-arm64.zip

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-arm64
          path: simple_live_app/build/macos/Build/Products/Release/simple_live_app-macos-arm64.zip
